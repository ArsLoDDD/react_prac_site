import React from 'react';
import styles from './Home.module.scss'

const Home = () => {
    return (
        <h1 className={styles.headName}>
            Home
        </h1>
    );
}

export default Home;
