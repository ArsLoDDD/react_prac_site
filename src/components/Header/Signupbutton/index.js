import React from "react";
import styles from './Signup.module.scss'

const Signup = (props) => {
  return (
      <div className={styles.headerSingUp}><span className={styles.span}>Signup</span></div>
  );
};

export default Signup;
